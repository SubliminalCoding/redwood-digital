[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Keep Netlify Forms submissions working (the contact form uses data-netlify="true")
# and route the chat function at /api/chat via the function's own config.path.
